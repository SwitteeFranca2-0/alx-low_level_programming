#include <stdio.h>
#include "main.h"

/**
 * _abs - computes absolute values.
 * @n: variable.
 * Return: returns the value, n.
 */
int _abs(int n)
{
	if (n < 0)
	{
		n = n * -1;
		return (n);
	}
	else
	{
		return (n);
	}
}
