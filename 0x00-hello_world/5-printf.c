#include <stdio.h>

/**
 * main - printing an array of characters.
 *
 * Return: Always a value 0.
 */

int main(void)
{
	char print_line[] = "with proper grammar, but the outcome is a piece of art,";

	printf("%s\n", print_line);
	return (0);
}
