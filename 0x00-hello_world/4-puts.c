#include <stdio.h>

/**
 * main - prints a string or array of characters.
 *
 * Return:  returning output zero.
 */
int main(void)
{
	char c[] = "\"Programming is like building a multilingual puzzle.\n";

	puts(c);
return (0);
}
